#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, x;
    cin >> n >> x;
    set<pair<int, int>> s;
    for(int i = 0; i < n; i++){
        int temp;
        cin >> temp;
        set.insert({temp, i});
    }
}